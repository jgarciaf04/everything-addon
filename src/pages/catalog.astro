---
import type { Addon } from '../types/addon';
import NavHeader from '../components/NavHeader.astro';
import '../styles/global.css';
import addons from '../data/addons.json';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" />
  <title>Catalog — Everything Addon</title>
</head>
<body>
  <NavHeader />

  <main class="catalog-page">
    <!-- ── Page Heading ─────────────────────────────────────────────────── -->
    <div class="catalog-header">
      <h1 class="catalog-title">Addon Catalog</h1>
      <!-- Decorative pixel-art diamond sword -->
      <div class="pixel-art-sword" aria-hidden="true"></div>
    </div>

    <!-- ── Filter / Search Bar ──────────────────────────────────────────── -->
    <div class="filter-bar">
      <input
        id="search-input"
        class="search-input"
        type="text"
        placeholder="Search addons..."
        aria-label="Search addons by name, description, or tags"
      />

      <div class="filter-buttons" role="group" aria-label="Filter by addon type">
        <button class="filter-btn filter-btn--active" data-type="all" type="button" aria-pressed="true">All</button>
        <button class="filter-btn" data-type="mob" type="button" aria-pressed="false">Mob</button>
        <button class="filter-btn" data-type="item" type="button" aria-pressed="false">Item</button>
        <button class="filter-btn" data-type="block" type="button" aria-pressed="false">Block</button>
        <button class="filter-btn" data-type="system" type="button" aria-pressed="false">System</button>
      </div>
    </div>

    <!-- ── Addon Card Grid ──────────────────────────────────────────────── -->
    <div class="card-grid">
      {addons.map((addon: Addon) => (
        <div
          class="addon-card"
          data-type={addon.type}
          data-name={addon.name}
          data-description={addon.description.short}
          data-tags={addon.tags.join(',')}
          data-id={addon.id}
          data-slug={addon.slug}
        >
          <a class="card-link" href={'/addons/' + addon.slug}>
            <img src={addon.thumbnail} alt={addon.name} class="card-thumb" />
            <div class="card-body">
              <span class="type-badge">{addon.type}</span>
              <h3 class="card-name">{addon.name}</h3>
              <p class="card-desc">{addon.description.short}</p>
              <span class="card-price">&euro;{addon.price.toFixed(2)}</span>
            </div>
          </a>
          <button class="cart-btn" type="button" data-addon-id={addon.id}>Add to Cart</button>
        </div>
      ))}

      <!-- ── Create Your Own Item CTA Card ────────────────────────────────── -->
      <a class="cta-card" href="/create">
        <div class="cta-card-icon" aria-hidden="true">&#10024;</div>
        <div class="cta-card-body">
          <h3 class="cta-card-title">Create Your Own Item</h3>
          <p class="cta-card-desc">Use AI to generate a custom addon</p>
        </div>
        <span class="cta-card-arrow" aria-hidden="true">&#10132;</span>
      </a>
    </div>

    <p id="no-results" class="no-results" hidden>No addons match your search.</p>
  </main>
</body>
</html>

<style>
  /* ── Page Container ──────────────────────────────────────────────────── */

  .catalog-page {
    max-width: 1280px;
    margin: 0 auto;
    padding: var(--space-5) var(--space-4) var(--space-7);
  }

  /* ── Page Header ─────────────────────────────────────────────────────── */

  .catalog-header {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-5);
  }

  /* ── Page Title ──────────────────────────────────────────────────────── */

  .catalog-title {
    font-family: var(--font-heading);
    font-size: var(--text-2xl);
    color: var(--color-primary);
    margin-bottom: 0;
  }

  /* ── Pixel-Art Diamond Sword (16 rows x 5 cols, 6px per pixel) ─────── */

  .pixel-art-sword {
    flex-shrink: 0;
    width: 6px;
    height: 6px;
    opacity: 0.55;
    pointer-events: none;
    /* Element itself is transparent; all pixels rendered via box-shadow */
    background: transparent;
    box-shadow:
      /* Row 0 — blade tip */
      12px 0px 0 0 var(--color-oak),
      /* Row 1 — blade */
       6px 6px 0 0 var(--color-primary),
      12px 6px 0 0 var(--color-oak),
      /* Row 2 — blade */
      12px 12px 0 0 var(--color-primary),
      18px 12px 0 0 var(--color-oak),
      /* Row 3 — blade */
       6px 18px 0 0 var(--color-primary),
      12px 18px 0 0 var(--color-oak),
      /* Row 4 — blade */
      12px 24px 0 0 var(--color-primary),
      18px 24px 0 0 var(--color-oak),
      /* Row 5 — blade */
       6px 30px 0 0 var(--color-primary),
      12px 30px 0 0 var(--color-oak),
      /* Row 6 — blade */
      12px 36px 0 0 var(--color-primary),
      18px 36px 0 0 var(--color-oak),
      /* Row 7 — blade */
       6px 42px 0 0 var(--color-primary),
      12px 42px 0 0 var(--color-oak),
      /* Row 8 — blade lower */
      12px 48px 0 0 var(--color-primary),
      /* Row 9 — guard */
       6px 54px 0 0 var(--color-accent),
      12px 54px 0 0 var(--color-accent),
      18px 54px 0 0 var(--color-accent),
      /* Row 10 — guard wide */
       0px 60px 0 0 var(--color-accent),
       6px 60px 0 0 var(--color-accent),
      12px 60px 0 0 var(--color-accent),
      18px 60px 0 0 var(--color-accent),
      24px 60px 0 0 var(--color-accent),
      /* Row 11 — handle */
      12px 66px 0 0 var(--color-stone),
      /* Row 12 — handle */
      12px 72px 0 0 var(--color-stone),
      /* Row 13 — handle */
      12px 78px 0 0 var(--color-stone),
      /* Row 14 — pommel */
      12px 84px 0 0 var(--color-accent);
  }

  /* ── Filter / Search Bar ─────────────────────────────────────────────── */

  .filter-bar {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-5);
  }

  .search-input {
    flex: 1 1 240px;
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--color-text-light);
    background: var(--color-bg-mid);
    border: var(--space-1) solid var(--color-border);
    border-radius: var(--border-radius);
    padding: var(--space-2) var(--space-3);
    outline: none;
    transition: border-color 0.15s;
  }

  .search-input::placeholder {
    color: var(--color-stone);
  }

  .search-input:focus {
    border-color: var(--color-primary);
  }

  .filter-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .filter-btn {
    font-family: var(--font-heading);
    font-size: var(--text-xs);
    color: var(--color-text-light);
    background: var(--color-bg-mid);
    border: var(--space-1) solid var(--color-border);
    border-radius: var(--border-radius);
    padding: var(--space-2) var(--space-3);
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s;
    text-transform: capitalize;
  }

  .filter-btn:hover,
  .filter-btn:focus-visible {
    background: var(--color-primary);
    border-color: var(--color-moss);
  }

  .filter-btn--active {
    background: var(--color-primary);
    border-color: var(--color-moss);
    box-shadow: var(--space-1) var(--space-1) 0 var(--color-moss);
  }

  /* ── Card Grid ───────────────────────────────────────────────────────── */

  .card-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
  }

  @media (min-width: 640px) {
    .card-grid {
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    }
  }

  /* ── Addon Card ──────────────────────────────────────────────────────── */

  .addon-card {
    display: flex;
    flex-direction: column;
    background: var(--color-bg-mid);
    border: var(--space-1) solid var(--color-border);
    border-radius: var(--border-radius);
    overflow: hidden;
    transition: border-color 0.15s;
  }

  .addon-card:hover {
    border-color: var(--color-primary);
  }

  .card-link {
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .card-thumb {
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    display: block;
  }

  .card-body {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding: var(--space-3);
    flex: 1;
  }

  /* ── Type Badge ──────────────────────────────────────────────────────── */

  .type-badge {
    display: inline-block;
    align-self: flex-start;
    font-family: var(--font-heading);
    font-size: var(--text-xs);
    color: var(--color-text-dark);
    background: var(--color-accent);
    border-radius: var(--border-radius);
    padding: var(--space-1) var(--space-2);
    text-transform: capitalize;
    line-height: 1;
  }

  /* ── Card Text ───────────────────────────────────────────────────────── */

  .card-name {
    font-family: var(--font-heading);
    font-size: var(--text-sm);
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.4;
  }

  .card-desc {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-stone);
    margin: 0;
    line-height: 1.5;
    flex: 1;
  }

  .card-price {
    font-family: var(--font-heading);
    font-size: var(--text-sm);
    color: var(--color-accent);
  }

  /* ── Cart Button ─────────────────────────────────────────────────────── */

  .cart-btn {
    font-family: var(--font-heading);
    font-size: var(--text-xs);
    color: var(--color-text-light);
    background: var(--color-primary);
    border: none;
    border-top: var(--space-1) solid var(--color-moss);
    border-radius: var(--border-radius);
    padding: var(--space-2) var(--space-3);
    cursor: pointer;
    transition: background 0.15s;
    text-align: center;
  }

  .cart-btn:hover,
  .cart-btn:focus-visible {
    background: var(--color-accent);
  }

  .cart-btn--in-cart {
    background: var(--color-stone);
    border-top-color: var(--color-border);
    cursor: default;
    opacity: 0.8;
  }

  .cart-btn--in-cart:hover,
  .cart-btn--in-cart:focus-visible {
    background: var(--color-stone);
  }

  /* ── Create Your Own Item CTA Card ──────────────────────────────────── */

  .cta-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
    background: linear-gradient(
      135deg,
      var(--color-bg-mid) 0%,
      var(--color-moss) 100%
    );
    border: var(--space-1) solid var(--color-accent);
    border-radius: var(--border-radius);
    padding: var(--space-5) var(--space-4);
    text-decoration: none;
    color: inherit;
    transition: border-color 0.15s, box-shadow 0.15s, transform 0.15s;
    box-shadow:
      var(--space-1) var(--space-1) 0 var(--color-oak),
      inset 0 0 0 var(--space-1) rgba(240, 192, 64, 0.1);
    min-height: 260px;
    text-align: center;
    cursor: pointer;
  }

  .cta-card:hover,
  .cta-card:focus-visible {
    border-color: var(--color-primary);
    box-shadow:
      var(--space-1) var(--space-1) 0 var(--color-moss),
      inset 0 0 0 var(--space-1) rgba(45, 125, 210, 0.2);
    transform: translateY(calc(-1 * var(--space-1)));
  }

  .cta-card-icon {
    font-size: var(--text-3xl);
    line-height: 1;
  }

  .cta-card-body {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .cta-card-title {
    font-family: var(--font-heading);
    font-size: var(--text-sm);
    color: var(--color-accent);
    margin: 0;
    line-height: 1.4;
  }

  .cta-card-desc {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.5;
  }

  .cta-card-arrow {
    font-size: var(--text-lg);
    color: var(--color-accent);
    transition: transform 0.15s;
  }

  .cta-card:hover .cta-card-arrow {
    transform: translateX(var(--space-2));
  }

  /* ── No Results Message ─────────────────────────────────────────────── */

  .no-results {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    color: var(--color-stone);
    text-align: center;
    padding: var(--space-7) var(--space-4);
    margin: 0;
  }

  /* ── Responsive: mobile (375px+) ──────────────────────────────────────── */

  @media (max-width: 640px) {
    .catalog-page {
      padding: var(--space-4) var(--space-3) var(--space-6);
    }

    .catalog-title {
      font-size: var(--text-lg);
      margin-bottom: 0;
    }

    .catalog-header {
      margin-bottom: var(--space-4);
    }

    .pixel-art-sword {
      display: none;
    }

    .filter-bar {
      gap: var(--space-2);
      margin-bottom: var(--space-4);
    }

    .search-input {
      font-size: var(--text-sm);
      padding: var(--space-2);
    }

    .filter-btn {
      padding: var(--space-1) var(--space-2);
    }

    .card-body {
      padding: var(--space-2);
    }

    .card-name {
      font-size: var(--text-xs);
    }

    .card-desc {
      font-size: var(--text-xs);
    }

    .cta-card {
      padding: var(--space-4) var(--space-3);
      min-height: 200px;
    }

    .cta-card-icon {
      font-size: var(--text-2xl);
    }

    .cta-card-title {
      font-size: var(--text-xs);
    }

    .cta-card-desc {
      font-size: var(--text-xs);
    }
  }
</style>

<script>
  import { addToCart, isInCart, onCartChange } from '../scripts/cart';

  const searchInput = document.getElementById('search-input') as HTMLInputElement | null;
  const filterBtns = document.querySelectorAll('.filter-btn');
  const cards = document.querySelectorAll('.addon-card');
  const cartBtns = document.querySelectorAll('.cart-btn');
  const noResults = document.getElementById('no-results');

  let activeType = 'all';

  function filterCards() {
    if (!searchInput) return;
    const searchText = searchInput.value.toLowerCase();
    let visibleCount = 0;

    cards.forEach((card) => {
      const el = card as HTMLElement;
      const type = el.dataset.type || '';
      const name = (el.dataset.name || '').toLowerCase();
      const desc = (el.dataset.description || '').toLowerCase();
      const tags = (el.dataset.tags || '').toLowerCase();

      const matchesType = activeType === 'all' || type === activeType;
      const matchesSearch = !searchText || name.includes(searchText) || desc.includes(searchText) || tags.includes(searchText);

      const visible = matchesType && matchesSearch;
      el.style.display = visible ? '' : 'none';
      if (visible) visibleCount++;
    });

    if (noResults) {
      noResults.hidden = visibleCount > 0;
    }
  }

  if (searchInput) {
    searchInput.addEventListener('input', filterCards);
  }

  filterBtns.forEach((btn) => {
    btn.addEventListener('click', () => {
      filterBtns.forEach((b) => {
        b.classList.remove('filter-btn--active');
        b.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add('filter-btn--active');
      btn.setAttribute('aria-pressed', 'true');
      activeType = (btn as HTMLElement).dataset.type || 'all';
      filterCards();
    });
  });

  // Cart button handling
  function updateCartBtn(btn: Element) {
    const id = (btn as HTMLElement).dataset.addonId;
    if (!id) return;
    if (isInCart(id)) {
      btn.textContent = 'In Cart';
      btn.classList.add('cart-btn--in-cart');
      (btn as HTMLButtonElement).disabled = true;
    }
  }

  cartBtns.forEach((btn) => {
    updateCartBtn(btn);
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const id = (btn as HTMLElement).dataset.addonId;
      if (!id) return;
      addToCart(id);
      updateCartBtn(btn);
    });
  });

  onCartChange(() => {
    cartBtns.forEach((btn) => updateCartBtn(btn));
  });
</script>
